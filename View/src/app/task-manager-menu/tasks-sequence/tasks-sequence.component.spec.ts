import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TasksSequenceComponent } from './tasks-sequence.component';
/*
describe('TasksSequenceComponent', () => {
  let component: TasksSequenceComponent;
  let fixture: ComponentFixture<TasksSequenceComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [TasksSequenceComponent]
    });
    fixture = TestBed.createComponent(TasksSequenceComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/